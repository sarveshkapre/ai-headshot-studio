# PROJECT

## Commands
```bash
make setup
make dev
make test
make lint
make typecheck
make build
make check
make smoke
make release
```

## Environment
- Python 3.11+

## Next 3 improvements
1. Add face-guided crop using a lightweight detector.
2. Harden server-side upload validation and return structured error codes for better UX.
3. Add a batch CLI helper for non-UI workflows (process a folder to outputs/ + optional ZIP).
