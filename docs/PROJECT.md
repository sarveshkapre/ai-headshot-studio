# PROJECT

## Commands
```bash
make setup
make dev
make test
make lint
make typecheck
make build
make check
make smoke
make release
```

## Environment
- Python 3.11+

## Next 3 improvements
1. Add face-guided crop using a lightweight detector.
2. Add a batch CLI helper for non-UI workflows (process a folder to outputs/ + optional ZIP).
3. Add “profile suggestions” (auto-name saved profiles based on use-case/preset/style) to reduce friction.
